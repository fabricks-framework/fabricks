---
# yaml-language-server: $schema=../../../../../job-schema-bronze.json
- job:
    step: bronze
    topic: memory
    item: scd1
    tags: [run_1, run_2]
    options:
      mode: append
      uri: abfss://fabricks@$datahub/raw/monarch
      parser: monarch
      keys: [id]
- job:
    step: bronze
    topic: memory
    item: scd2
    tags: [run_1, run_2]
    options:
      mode: append
      uri: abfss://fabricks@$datahub/raw/monarch
      parser: monarch
      keys: [id]
