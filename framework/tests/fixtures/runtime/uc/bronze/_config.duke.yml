---
# yaml-language-server: $schema=../../../../../../job-schema-bronze.json
- job:
    step: bronze
    topic: duke
    item: latest
    tags: [run_1]
    options:
      mode: register
      uri: abfss://test@$datahub/raw/3/delta/duke
      keys: [id]
- job:
    step: bronze
    topic: duke
    item: append
    tags: [run_1]
    options:
      mode: register
      uri: abfss://test@$datahub/raw/4/delta/duke
      keys: [id]