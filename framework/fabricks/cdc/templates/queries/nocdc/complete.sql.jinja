__final as (
    select
      {% for column in columns %} {{ column }},
      {% endfor %},
    from {{ parent_cdc }}
    {% if filter %}
        where
            true
            -- operation current added by filter
            and __operation <> 'current'
    {% endif %}
)
